name: western_malabar
description: Western Malabar – Customer App (Flutter + Supabase)
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # 🟣 Supabase backend
  supabase_flutter: ^2.6.0

  # 🪴 Riverpod state management
  hooks_riverpod: ^2.5.1
  flutter_hooks: ^0.20.5

  # 🌍 Internationalization
  intl: ^0.19.0

  # 🖼️ Cached images for banners / product grids
  cached_network_image: ^3.3.1

  # 🧩 Utils and helpers
  provider: ^6.1.2
  shared_preferences: ^2.3.2
  url_launcher: ^6.3.1

  # 🧠 Rename helper for app id / package name edits
  rename: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # ✅ Fix lint issue (analysis_options.yaml)
  flutter_lints: ^4.0.0

  # 🟡 Launcher icon generator
  flutter_launcher_icons: ^0.13.1

  # 🟢 Native splash generator
  flutter_native_splash: ^2.4.1

# -------------------- FLUTTER LAUNCHER ICONS --------------------
flutter_launcher_icons:
  android: true
  ios: true
  image_path_android: assets/icon/fg.png
  image_path_ios: assets/icon/fg.png
  adaptive_icon_foreground: assets/icon/fg.png
  adaptive_icon_background: assets/icon/bg.png
  remove_alpha_ios: true

# -------------------- FLUTTER --------------------
flutter:
  uses-material-design: true
  assets:
    # ICONS
    - assets/icon/fg.png
    - assets/icon/bg.png

    # SPLASH (points to your /icon/ directory)
    - assets/icon/wm_mark.png
    - assets/icon/wm_mark_mono.png

# -------------------- FLUTTER NATIVE SPLASH --------------------
flutter_native_splash:
  color: "#FFF7E6"
  image: assets/icon/wm_mark.png

  android_12:
    color: "#FFF7E6"
    image: assets/icon/wm_mark_mono.png

  android: true
  ios: false
